# Assets

Drop a screenshot of the overlay here named `overlay.png`, then uncomment the
image line in the top-level `README.md` (the "Screenshot" section). A good shot is
the overlay on a map or card-reward screen showing a recommendation.
